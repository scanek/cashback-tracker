version: '3.8'

services:
  cashback-hub-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cashback-hub-web
    restart: always
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
